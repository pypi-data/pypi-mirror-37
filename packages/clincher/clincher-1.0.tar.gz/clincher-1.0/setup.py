#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['GitPython', 'dateparser']

entry_points = \
{'console_scripts': ['clincher = clincher:main']}

setup(name='clincher',
      version='1.0',
      description='Clincher is a tool for checking that all the commits in a git repo are signed',
      author='Tom Parker-Shemilt',
      author_email='palfrey@tevp.net',
      url='https://github.com/lshift/clincher',
      py_modules=['clincher'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6,<4',
     )
