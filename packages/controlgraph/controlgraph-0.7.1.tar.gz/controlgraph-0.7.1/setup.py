#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['networkx', 'pydot', 'python-debian']

entry_points = \
{'console_scripts': ['controlgraph = controlgraph:main']}

setup(name='controlgraph',
      version='0.7.1',
      description='Generate build order graph from directory of debian packaging repositories',
      author='Dell inc.',
      author_email='opx-infra@openswitch.net',
      url='https://openswitch.net',
      py_modules=['controlgraph'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.5,<4',
     )
