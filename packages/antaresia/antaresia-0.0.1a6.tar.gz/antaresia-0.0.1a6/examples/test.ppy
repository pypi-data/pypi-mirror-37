__CONSTANT = 7 # this is ignored

# antaresia functions can (but do not have to) be imported to make it easier to edit files in a
# Python IDE. On runtime this import is ignored like all the others.
from antaresia.config_functions import include_config, read

include_config('test.2.ppy')

def func() -> int:
    return 42

var_1 = __CONSTANT
var_2 = 2
var_3 = func()
var_read = read('theraven.txt')