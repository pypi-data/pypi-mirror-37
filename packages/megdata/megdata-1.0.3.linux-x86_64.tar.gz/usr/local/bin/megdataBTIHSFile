#!/usr/bin/python3

import os
import sys

from megdata import BTIHSFile

if len(sys.argv) != 2:
    print("Usage: %s FILENAME" % (os.path.basename(sys.argv[0])))
    sys.exit(1)

print(BTIHSFile.from_file(sys.argv[1]))
