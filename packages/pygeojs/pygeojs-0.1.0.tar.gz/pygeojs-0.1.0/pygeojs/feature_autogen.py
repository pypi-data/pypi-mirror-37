import six
from ipywidgets import Widget, DOMWidget, widget_serialization, Color, register
from traitlets import (
    Unicode, Int, CInt, Instance, ForwardDeclaredInstance, This, Enum,
    Tuple, List, Dict, Float, CFloat, Bool, Union, Any,
    )

from .sceneObject import sceneObject


@register
class feature(sceneObject):
    """feature

    Autogenerated by generate-wrappers.js
    """

    def __init__(self, **kwargs):
        super(feature, self).__init__(**kwargs)

    _model_name = Unicode('featureModel').tag(sync=True)

    _data = List().tag(sync=True)

    bin = CFloat(None, allow_none=True).tag(sync=True)

    data = List().tag(sync=True)

    gcs = Unicode(None, allow_none=True).tag(sync=True)

    selectionAPI = Bool(False, allow_none=False).tag(sync=True)

    style = Dict(default_value={}, allow_none=False).tag(sync=True)

    visible = Bool(True, allow_none=False).tag(sync=True)


if six.PY3:
    import inspect
    # Include explicit signature since the metaclass screws it up
    feature.__signature__ = inspect.signature(feature.__init__)
