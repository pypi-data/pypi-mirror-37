import six
from ipywidgets import Widget, DOMWidget, widget_serialization, Color, register
from traitlets import (
    Unicode, Int, CInt, Instance, ForwardDeclaredInstance, This, Enum,
    Tuple, List, Dict, Float, CFloat, Bool, Union, Any,
    )

from .layer_autogen import layer


class featureLayer(layer):
    """featureLayer

    Autogenerated by generate-wrappers.js
    """

    def __init__(self, map_id='', **kwargs):
        kwargs['map_id'] = map_id
        super(featureLayer, self).__init__(**kwargs)

    _model_name = Unicode('featureLayerModel').tag(sync=True)


if six.PY3:
    import inspect
    # Include explicit signature since the metaclass screws it up
    featureLayer.__signature__ = inspect.signature(featureLayer.__init__)
