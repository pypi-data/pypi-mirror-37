import six
from ipywidgets import Widget, DOMWidget, widget_serialization, Color, register
from traitlets import (
    Unicode, Int, CInt, Instance, ForwardDeclaredInstance, This, Enum,
    Tuple, List, Dict, Float, CFloat, Bool, Union, Any,
    )

from .feature_autogen import feature


@register
class pointFeature(feature):
    """pointFeature

    Autogenerated by generate-wrappers.js
    """

    def __init__(self, **kwargs):
        super(pointFeature, self).__init__(**kwargs)

    _model_name = Unicode('pointFeatureModel').tag(sync=True)

    dynamicDraw = Bool(False, allow_none=False).tag(sync=True)

    position = List().tag(sync=True)

    primitiveShape = Unicode("sprite", allow_none=False).tag(sync=True)

    style = Dict(default_value={}, allow_none=False).tag(sync=True)


if six.PY3:
    import inspect
    # Include explicit signature since the metaclass screws it up
    pointFeature.__signature__ = inspect.signature(pointFeature.__init__)
